import React from "react";
import styles from "./Footer.module.css"; // Import file CSS riêng

const Footer = () => {
    return (
        <footer className={styles.footer}>
        </footer>
    );
};

export default Footer;